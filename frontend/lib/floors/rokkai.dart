import 'package:flutter/material.dart';

class Rokkai extends StatelessWidget {
  const Rokkai({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text('Rokkai'),
          ],
        ),
      );
  }
}
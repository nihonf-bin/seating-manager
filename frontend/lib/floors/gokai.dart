import 'package:flutter/material.dart';

class Gokai extends StatelessWidget {
  const Gokai({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Gokai'),
      );
  }
}